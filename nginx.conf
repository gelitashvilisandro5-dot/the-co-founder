worker_processes 1;

events { worker_connections 1024; }

http {
    include    mime.types;
    sendfile on;
    
    # File upload limit - 100MB
    client_max_body_size 100M;

    server {
        # Cloud Run მოითხოვს 8080 პორტს
        listen 8080;
        server_name localhost;

        root /var/www/html;
        index index.php index.html;

        location /assets/ {
            try_files $uri =404;
            expires 1y;
            access_log off;
            add_header Cache-Control "public";
        }

        location / {
            try_files $uri $uri/ /index.php?$query_string;
            add_header Cache-Control "no-store, no-cache, must-revalidate";
        }

        # STREAMING API OPTIMIZATION
        location = /ask_expert_api.php {
            fastcgi_pass 127.0.0.1:9000;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            
            # CRITICAL: Disable buffering for SS
            fastcgi_buffering off;
            gzip off;
            
            # Prevent stale browser sockets by forcing a fresh connection for the API
            keepalive_timeout 0;
            
            # Extended timeouts for long AI thoughts
            fastcgi_read_timeout 600s;
            fastcgi_send_timeout 600s;
        }

        location ~ \.php$ {
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;
            
            # Timeout settings for large files & AI processing
            fastcgi_read_timeout 300s;
            fastcgi_send_timeout 300s;
            fastcgi_connect_timeout 60s;
            fastcgi_buffers 16 16k;
            fastcgi_buffer_size 32k;
        }
    }
}